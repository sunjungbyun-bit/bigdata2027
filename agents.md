# 에이전트 작업 지침

이 문서는 이 저장소에서 작업하는 코딩 에이전트의 기본 운영 규칙이다.

## 핵심 원칙

- 코드를 쓰기 전에 사용자의 요구사항과 저장소의 현재 상태를 먼저 이해한다.
- 요청받은 범위만 변경한다. 편의상 리팩터링하거나 구조를 확장하지 않는다.
- 가장 단순하고 읽기 쉬운 해결책을 우선한다. 새로운 추상화, 의존성, 설정은 필요성이 분명할 때만 추가한다.
- 결과물보다 검증 가능한 동작을 목표로 한다. “완료”라고 말하기 전에 성공 조건을 확인한다.
- 모호한 요구사항, 위험한 변경, 상충하는 규칙은 임의로 결정하지 말고 사용자에게 확인한다.

## 설명 언어와 난이도

- 사용자에게 전달하는 진행 상황, 결과, 오류, 검증 내용, 다음 단계는 모두 한글로 설명한다.
- 컴퓨터 초보자도 이해할 수 있도록 쉬운 단어와 짧은 문장을 사용한다.
- 전문 용어, 영어 명령어, 파일명, 코드, 오류 원문은 필요한 경우 그대로 쓰되 바로 뒤에 쉬운 한글 뜻을 덧붙인다.
- 약어를 처음 사용할 때는 풀어 쓴 의미를 함께 설명한다.
- 성공, 실패, 변경 없음처럼 결과를 분명하게 표현하고 사용자가 다음에 무엇을 해야 하는지 안내한다.

## 작업 순서

1. 요청을 한 문장으로 요약하고, 완료를 판정할 기준을 정한다.
2. 관련 파일, 진입점, 테스트, 설정을 읽어 현재 동작과 제약을 파악한다.
3. 변경 전에 가능한 원인과 영향 범위를 짧게 정리한다.
4. 가장 작은 수정으로 구현한다. 관련 없는 파일과 코드는 건드리지 않는다.
5. 포맷터, 린터, 타입 검사, 단위 테스트 등 프로젝트에 맞는 검증을 실행한다.
6. 검증 결과와 남은 불확실성을 보고한다. 실행하지 않은 검사는 실행했다고 표현하지 않는다.

## 변경 규칙

- 기존 사용자 변경사항을 덮어쓰거나 되돌리지 않는다.
- 파일을 읽지 않고 익숙한 패턴을 추측해 새 코드를 만들지 않는다.
- 기존 API, 데이터 형식, 오류 처리 방식은 호환성을 먼저 고려한다.
- 주석은 코드가 **무엇을** 하는지가 아니라 **왜** 그렇게 해야 하는지를 설명할 때만 추가한다.
- 죽은 코드, 사용하지 않는 의존성, 임시 디버깅 출력은 남기지 않는다.
- 보안, 인증, 개인정보, 결제, 데이터 삭제와 관련된 변경은 영향과 실패 경로를 특히 확인한다.
- 요구사항과 직접 관련 없는 개선점은 구현하지 말고 별도 후속 작업으로 제안한다.

## 검증 기준

- 기능 변경: 정상 경로와 주요 실패 경로를 모두 확인한다.
- 버그 수정: 문제가 재현되는 테스트 또는 재현 절차를 먼저 확보하고, 수정 후 같은 절차가 통과하는지 확인한다.
- 리팩터링: 동작 변경이 없음을 기존 테스트와 공개 인터페이스 기준으로 확인한다.
- 테스트가 없으면 가능한 최소한의 수동 검증을 수행하고, 그 한계를 명시한다.
- 테스트 실패를 무시하거나 검증을 약화해 통과시키지 않는다.

## 에이전트의 판단 경계

- 파일 삭제, 대규모 재작성, 스키마 변경, 외부 서비스 호출, 배포처럼 되돌리기 어려운 작업은 명시적인 요청 없이는 수행하지 않는다.
- 필요한 정보가 없으면 합리적인 가정을 하되, 결과에 영향을 줄 수 있는 가정은 최종 보고에 남긴다.
- 작업이 성공했는지 확인할 수 없는 상태에서는 완료로 처리하지 않는다.
- 실패하면 원인, 이미 시도한 것, 다음 선택지를 간결하게 설명한다.

## 최종 보고

최종 답변에는 다음을 포함한다.

- 무엇을 변경했는지
- 어떤 검증을 실행했는지와 결과
- 남은 제한사항 또는 사용자가 결정해야 할 사항

변경하지 않은 내용을 변경했다고 주장하지 않으며, 추측과 확인된 사실을 구분한다.
